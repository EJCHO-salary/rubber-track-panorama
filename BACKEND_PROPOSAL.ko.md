# 러버트랙 사진 전개 백엔드 설계 제안

> 아래는 최초 설계 기록입니다. 이후 사용자의 요청에 따라 실제 프로토타입을 구현했습니다. 현재 입력은 폭·피치 필수, 링크 수 선택이며, 링크 수가 없으면 제공된 사진 범위만 처리합니다. 실행 방법과 현재 지원 범위는 [README.md](README.md)를 기준으로 확인하세요.

작성 기준: `ref_img/11.jpg`–`15.jpg` 직접 관찰 및 OpenCV 특징점 진단.
제품 정보: 폭 **450 mm**, 피치 **86 mm**. 사용자의 ‘86pitch’를 피치 간격 86 mm로 해석했다.
전체 피치 수 N은 아직 제공되지 않았다.

이 문서는 설계 제안이다. 실행한 코드는 사진 대응 관계를 분석하는 진단 도구이며, 최종 파노라마 생성 백엔드는 아직 구현하지 않았다.

## 1. 권장 방식

**사진마다 피치 위치를 식별하고, 실제 폭·피치를 기준으로 공통 좌표계에 펼친 다음, 같은 위치의 여러 관측 중 가장 좋은 원본을 선택해 합성한다.**

최종 결과는 장비에 장착된 러버트랙의 외측 표면을 길게 펼친 한 장의 전개 사진이다. 표면의 균열·마모·파손을 유지한다. 보이지 않는 부분을 생성해 채우지 않는다.

기본 단위는 ‘사진’이 아니라 **제품의 고유 피치 구간**이다. 사진은 해당 구간을 관측한 자료이며, 한 구간에 여러 사진이 대응할 수 있다. 최종 전개도에는 해당 구간이 한 번만 존재한다.

```text
원본 + 제품 규격
  → 방향·렌즈 보정 / 화질 검사
  → 트랙 마스크 / 기준 폭 / 피치 기준점 검출
  → 동일 피치 식별 / 사진 순서와 전체 연결 관계 추정
  → 폭·피치 제약을 적용한 전개 좌표 계산
  → 작은 영역별 원본 선택 / 경계 최적화 / 제한적인 색 보정
  → 중복·누락·변형·이음새 검사
  → 전개 사진 + 원본 추적 정보 + 품질 보고서
```

## 2. 이번 사진에서 확인한 것

| 관찰 또는 측정 | 설계에 미치는 영향 |
| --- | --- |
| 원본 파일 크기는 4000×3000, EXIF 방향 적용 후 3000×4000 | 시작 단계에서 방향을 한 번만 적용해야 한다. |
| 배경은 비슷하고 트랙의 번호·균열이 이동 | 트랙을 회전시키며 촬영한 것으로 보인다. 배경과 제품의 이동 모델을 분리해야 한다. |
| 인접 사진에서 같은 표면 특징을 검출 | 5장을 연결할 수 있는 근거가 있다. 전체 원주의 완전한 촬영을 뜻하지는 않는다. |
| 비슷한 돌기·중앙 구조가 반복 | 한 피치 또는 두 피치가 잘못 밀리는 오류를 별도로 막아야 한다. |
| 왼쪽에 넓은 그림자, 중앙에 돌출·깊이 변화 | 전역 변환 하나와 밝기 평균만으로는 전폭 정렬과 자연스러운 이음새를 보장하기 어렵다. |
| 분필 번호, 개별 균열, 중앙 구조의 마모가 보임 | 반복 모양 외에 동일 위치를 식별할 단서로 사용한다. |

### 실행한 진단

Pillow로 EXIF 방향을 적용하고 1200×1600으로 줄여 SIFT를 추출했다. 트랙 내부의 보수적인 직사각형 영역과 양 끝의 배경 영역을 별도로 분석했다. 양방향 일치와 ratio 0.72로 대응점을 걸러낸 뒤 RANSAC 임계값 3 px로 affine/homography를 각각 추정했다. 모든 사진 쌍 10개를 확인했다.

아래 표는 homography의 진단 결과다. **정답 라벨이나 독립적인 측정 정확도 검증 결과가 아니다.**

| 사진 쌍 | 트랙 모델 내점 수 | 내점 적합 오차 중앙값 | 세로 이동량 기반 겹침 근사 |
| --- | ---: | ---: | ---: |
| 11 → 12 | 52 | 1.204 px | 50.9% |
| 12 → 13 | 73 | 0.960 px | 39.2% |
| 13 → 14 | 61 | 1.078 px | 38.5% |
| 14 → 15 | 112 | 0.576 px | 45.7% |

오차 단위는 1200×1600 분석 이미지의 픽셀이다. 원본 좌표의 픽셀 값으로 환산하면 2.5배지만, 이것도 실제 치수 정확도는 아니다. 겹침 근사는 `1 - |내점의 세로 이동 중앙값| / 1600`이며, 유효 중첩 마스크의 면적을 측정한 값이 아니다.

인접 사진의 배경 내점은 각각 115, 152, 196, 206개이고 적합 오차도 작았다. 즉, 배경을 포함한 매칭은 제품보다 배경에 잘 맞을 수 있다. 모든 인접 사진 쌍에서 선택된 트랙 homography 내점은 x<480 영역에 없었다. 사진 왼쪽의 정렬 품질까지 이 결과로 승인하면 안 된다. 그림자 변화·돌출부의 시차 등은 가능한 원인으로, 현재 진단만으로 원인을 분리하지는 않았다.

비인접 쌍에서는 트랙의 강한 연결 근거를 얻지 못했다. 일부 쌍은 4~5개 내점만으로 수학적인 변환이 만들어졌는데, 이를 정상 연결로 취급하면 안 된다. 11번과 15번 사이의 원주 폐합도 이 진단으로 확인되지 않았다.

원본을 수정하지 않았다. 재현 도구는 `analysis/inspect_tracks.py`, 원본 해시와 대응점·행렬을 포함한 결과는 `analysis/measurements.json`에 있다.

## 3. 업로드 시 받을 정보

| 항목 | 권장 정책 |
| --- | --- |
| 제품/촬영 세션 ID, 좌·우 트랙 구분 | 서로 다른 제품 또는 좌·우 트랙이 같은 작업으로 섞이지 않게 한다. |
| 실제 폭 `width_mm` | 필수. 이번 제품은 450. |
| 피치 `pitch_mm` | 필수. 이번 제품은 86. 단위를 명시한다. |
| 전체 피치 수 `total_pitch_count` | 전체 한 바퀴의 완전성 검증을 요청하면 입력 권장. 모르면 부분 결과만 허용하거나 완전성 미확인으로 표시한다. |
| 원본 이미지 | 필수. 메신저 압축본보다 카메라 원본을 우선한다. |
| 범위 `coverage_mode` | `full_loop` 또는 `partial`. |
| 시작 표시와 회전 방향 | 선택. 입력하면 순서·폐합 판별이 쉬워진다. |
| 카메라 보정 프로필 | 관리자가 촬영 장치별로 등록. 사용자가 매번 입력하지 않게 한다. |

제품 카탈로그에 규격을 저장하고 제품을 선택하면 폭·피치·N이 자동으로 채워지도록 한다. N을 모를 때 임의의 수치를 기본값으로 넣지 않는다.

## 4. 단계별 핵심 로직

### A. 입력 정리와 촬영 품질 검사

- 원본 해시, 촬영 순서, EXIF, 방향을 기록한다. 파일명 순서는 초기 후보로만 사용한다.
- EXIF 회전과 카메라 왜곡 보정을 명확히 분리한다. EXIF만으로 렌즈 보정이 완료되지는 않는다.
- 같은 카메라·렌즈·해상도·촬영 모드로 얻은 보정 프로필이 있을 때 렌즈 왜곡을 보정한다. 프로필이 없으면 보정 정확도 미확인 상태를 남긴다.
- 흔들림, 초점, 포화, 그림자, 폭 가장자리 잘림, 유효 영역을 검사한다. 사진 전체뿐 아니라 피치·폭 위치별 품질을 계산한다.
- 저해상도로 위치를 찾고 원본 해상도로 합성한다. 기하 변환들을 합성한 후 원본을 한 번 재표본화해 반복 보간에 따른 선명도 저하를 줄인다.

### B. 트랙 영역과 구조 검출

- 배경, 차체, 바닥, 사람을 제품 정렬 대상에서 제외한다. 배경 매칭은 카메라 흔들림 진단에만 보조적으로 활용한다.
- 양쪽 기준 가장자리, 중심선, 중앙 반복 구조, 좌우 돌기 기준점을 찾는다.
- 초기 시제품은 이미지마다 마스크·양끝 선·몇 개의 피치 기준점을 수동 보정할 수 있도록 한다. 검증된 라벨을 모아 전용 분할/키포인트 모델로 자동화한다.
- 패턴의 반복 주기가 실제 규격 피치의 1배인지, 2배인지, 절반인지 규격·중앙 구조·번호를 함께 보고 결정한다.
- **기준 폭은 정상적인 외곽을 따라 추정한 명목 경계로 잡는다.** 뜯겨나간 가장자리를 매 행마다 450 mm로 늘리면 파손 형상이 변한다. 실제 결손 윤곽은 별도 마스크로 유지한다.

### C. 동일한 물리 위치 식별과 중복 방지

1. 제품 마스크 안에서 인접 후보들의 특징점을 매칭한다. SIFT + 양방향 검사 + RANSAC을 첫 기준선으로 사용한다.
2. 한 가지 이동량으로 즉시 결정하지 않고, 피치 단위로 밀린 여러 후보를 유지한다.
3. 균열·마모·흙 자국의 배열, 중앙 구조, 좌우 돌기, 촬영 순서가 같은 위치를 지지하는지 검사한다.
4. 분필 OCR은 보조 근거로 쓴다. 손글씨 오인식만으로 피치 번호를 확정하지 않는다.
5. 여러 사진을 연결한 그래프에서 순서와 이동량이 일관되는 후보를 선택한다. 불확실성이 큰 간선은 연결하지 않는다.
6. 전체 피치에 `physical_pitch_id`를 부여한다. 같은 ID가 여러 사진에 나타나도 출력 좌표는 하나다.
7. 한 바퀴가 확인되면 마지막과 첫 관측의 ID를 통합한다. 전개 좌표에서는 원주 길이 `N×P` 차이를 갖고 같은 위치가 된다는 주기 조건을 적용한다.

학습 기반 매칭은 기준선이 실패하는 사례를 모은 뒤 비교 평가한다. 매칭 모델을 바꾸어도 피치 ID·순서·폐합 검증은 유지한다.

완전히 같은 무늬가 반복되고 고유한 마모·번호도 없다면, 사진만으로 이동량이 몇 피치인지 유일하게 결정할 수 없는 경우가 있다. 이 경우 시작점 및 일정 간격의 고유 표식, 또는 회전량을 기록한 엔코더처럼 추가 근거가 필요하다. 알고리즘의 자신감만 높여 이 모호성을 해결할 수는 없다.

### D. 폭·피치를 맞추는 전개 좌표

폭 방향을 u, 진행 방향을 v로 정의한다. 명목 좌표는 `0 ≤ u ≤ W`, `v = kP + tP`이며 k는 고유 피치 번호, t는 해당 피치 내부 위치다.

초기 지도에서는 아래처럼 정규화할 수 있다.

```text
u = W × (x - left(y)) / (right(y) - left(y))
v = P × [k + (y - anchor[k]) / (anchor[k+1] - anchor[k])]
```

이 식은 이미 대체로 정면 보정된 직선 구간의 초기 모델이다. 원본의 기울기·휘어짐·폭 방향 시차까지 해결하는 완성 모델은 아니다. 실제 구현에서는 좌·중앙·우측 기준점을 가진 2D mesh로 보정하고, 모든 사진을 같은 전개 좌표에 동시에 맞춘다.

```text
최적화 비용 = 대응점 오차
            + 피치 기준점과 공통 격자의 오차
            + 명목 폭 경계 오차
            + 국소 변형의 매끄러움 / 과도한 늘어남 페널티
            + 전체 연결 그래프의 일관성 오차
```

- 전역 affine 또는 homography는 초기 위치 추정에 이용한다. 모든 사진에 변환을 순차 누적해 마지막까지 늘어뜨리는 구조는 피한다.
- mesh의 뒤집힘, 진행 방향 역전, 한 피치 단위의 갑작스러운 점프를 금지한다.
- 조밀한 optical flow는 ID가 확정된 같은 피치 내부의 작은 보정에만 제한하고, 과도한 변형은 거절한다.
- 약한 영역은 다른 원본이나 수동 기준점을 사용한다. 강한 영역의 변환을 무조건 외삽해서 품질을 승인하지 않는다.
- 출력 폭·피치가 균일하다는 사실과 실제 마모·늘어남을 정확하게 측정한다는 사실을 구분한다. 정규화된 전개 사진에서 실제 피치 변형을 역으로 측정하면 안 된다. 측정 목적에는 원본과 보정 정보가 별도로 필요하다.

예를 들어 출력 해상도를 5 px/mm로 정하면:

- 폭: `450×5 = 2250 px`
- 한 피치: `86×5 = 430 px`
- 한 바퀴의 길이: `N×430 px`

가로로 긴 결과는 `(N×430) × 2250 px`가 된다. 5 px/mm는 출력 예시이며, 실측 정확도가 0.2 mm라는 뜻이 아니다. 해상도는 원본의 유효 선명도와 자원 한도를 고려해 결정한다.

### E. 가장 좋은 원본 선택과 이음새 처리

- 각 피치의 여러 관측에 대해 선명도, 정면성, 그림자, 가림, 표면 정렬 신뢰도를 평가한다.
- 가능한 범위에서 하나의 원본이 연속 영역을 맡게 한다. 작은 픽셀마다 출처가 바뀌는 모자이크를 피한다.
- 같은 피치의 다른 관측으로 보완할 수 있으나, 서로 다른 피치의 텍스처를 섞지 않는다.
- 이음새는 원본 간 색·기울기 차이가 작고, 중요한 균열이나 돌기 모서리를 자르지 않는 경로로 선택한다.
- 정렬이 확인된 좁은 경계에서만 feather 또는 multiband blending을 적용한다. 넓은 평균 합성으로 이중 균열이나 흐려진 돌기를 만들지 않는다.
- 겹치는 동일 표면에서 노출·화이트밸런스를 보정한다. 강한 그림자는 원본 선택으로 먼저 해결하고, 색 보정은 저주파 밝기 차이에 제한한다.
- 모든 관측에서 가려졌거나 날아간 부분은 자동 복원하지 않고 결손으로 표시한다.

### F. 전체 품질 판정

**출력 사진이 만들어졌다는 이유만으로 성공 처리하지 않는다.**

| 검사 | 판정 기준 |
| --- | --- |
| 중복 | 각 물리 피치 ID가 출력에서 정확히 한 구간을 차지하는가? |
| 누락 | 필요한 모든 피치와 폭 위치에 유효 원본 관측이 있는가? N과 폐합이 확인되었는가? |
| 폭·피치 격자 | 전개 좌표의 규격과 검출된 구조가 일치하는가? 출력 크기만 맞춘 것은 아닌가? |
| 전폭 정렬 | 좌·중·우에 독립적인 검증점 또는 신뢰할 기준점이 있는가? |
| 결함 보존 | 원본의 대표 균열·마모가 한 번씩 유지되는가? 이음새에서 끊기거나 복제되지 않았는가? |
| 국소 변형 | 뒤집힌 mesh, 과도한 면적 변화, 피치 점프가 없는가? |
| 색·경계 | 이중 모서리, 노출 불연속, 가짜 결함이 없는가? |

초기 임계값은 여러 제품의 사람이 확인한 정답 세트에서 정한다. 이번 5장만 보고 ‘오차 1% 이하’ 같은 품질 보증 수치를 정하지 않는다. 적합에 사용하지 않은 검증점과 별도 측정 표식을 사용해 평가한다.

작업 상태는 `complete`, `partial`, `needs_review`, `failed`로 구분한다. 완전성 정보는 `coverage_verified`로 별도 기록한다. 매칭이 약한 사진 쌍은 해당 중첩 구간만 다시 찍거나 기준점을 보정하도록 안내한다.

## 5. 백엔드 구성

첫 구현은 Python + OpenCV + NumPy로 기하·합성의 기준선을 만들고, API 서버와 무거운 이미지 작업 프로세스를 분리한다. 작업 큐 뒤의 worker가 원본 저장소에서 읽고 결과·진단 자료를 저장한다. 분할/키포인트 모델은 동일 인터페이스로 추후 교체한다.

```text
POST /track-unwrapping/jobs             규격과 원본 목록 등록 → job_id
GET  /track-unwrapping/jobs/{id}         진행률, 상태, 검토 필요 사유
GET  /track-unwrapping/jobs/{id}/result  결과 이미지와 품질 자료
PATCH /track-unwrapping/jobs/{id}/anchors  기준점·ID 수정 → 재처리 작업
```

입력 예시:

```json
{
  "product_id": "track-001",
  "width_mm": 450,
  "pitch_mm": 86,
  "total_pitch_count": null,
  "coverage_mode": "full_loop",
  "output_px_per_mm": 5,
  "images": ["11.jpg", "12.jpg", "13.jpg", "14.jpg", "15.jpg"]
}
```

N이 null인 이 요청은 처리를 시작할 수 있지만, 검증된 폐합과 피치 수를 확보하기 전에는 전체 원주 완성으로 승인하지 않는다.

결과물:

- 전개도 PNG/TIFF 및 가벼운 미리보기.
- 피치별 원본 사진·원본 좌표·변환·채택 이유를 담은 `provenance.json`.
- 픽셀 또는 타일 단위 원본 선택 지도, 유효 영역/미관측 영역 마스크.
- 중복·누락·정렬 근거·검토 사유를 담은 `quality_report.json`.

큰 전개도는 타일 단위로 처리·저장하고, 원본 전체와 모든 임시 변형 이미지를 동시에 메모리에 올리지 않는다. 폭·피치·N·출력 해상도로 예상 크기를 먼저 계산한다. 재실행 시 동일 원본·설정·모델 버전은 추적 가능하게 기록한다.

## 6. 구현 순서와 촬영 개선

1. **현재 샘플로 반자동 기준 결과 제작:** 트랙 마스크·피치 기준점·시작점을 사람이 확인하고, 공통 격자와 원본 선택 합성을 구현한다. 자동화 전에 원하는 품질 기준을 확보한다.
2. **여러 제품에서 자동화:** 손글씨가 없는 제품, 새 제품, 진흙·마모·강한 그림자, 순서가 섞이거나 중간이 빠진 입력을 수집한다. 피치 식별과 자동 거절 조건을 검증한다.
3. **작업 API와 검토 흐름 추가:** 불확실한 구간만 확인하고 재처리하는 구조를 만든다.

촬영 권장값은 초기 가이드이며, 제품과 카메라에 맞춰 조정한다.

- 같은 카메라·렌즈·거리·초점·노출·화이트밸런스를 유지하고, 가능하면 카메라를 고정한다.
- 외측 표면의 직선 구간을 정면으로 찍고, 접지부나 크게 휜 구간의 사진은 보조 관측으로 쓴다.
- 양쪽 끝까지 보이도록 약간의 여백을 두고, 이전 사진과 약 50~60%를 겹친다. 중앙 한두 줄만 사용할 계획이면 그 유효 영역 사이에도 겹침이 있어야 한다.
- 부드러운 조명 또는 큰 확산광으로 그림자 경계를 줄인다. 소프트웨어의 그림자 제거보다 촬영 개선 효과가 크다.
- 시작점을 표시하고, 한 바퀴 후 같은 표시가 다시 보이게 찍는다. 가능하면 전체 피치 수도 기록한다.
- 일정 간격의 번호나 임시 고유 표시를 활용하면 균일하게 반복되는 새 트랙의 한 피치 밀림 오류를 줄일 수 있다.

현재 사진은 부분 연결 실험에 유용하다. 그러나 전체 피치 수와 시작·끝 폐합이 확인되지 않았으므로 ‘한 제품 전체가 중복·누락 없이 완성된다’고 아직 확정할 수는 없다. 특히 강한 그림자나 깊은 틈에서 원본에 남지 않은 정보는 사진 합성만으로 복구할 수 없다.

## 7. 기술 근거

- [OpenCV: Feature Matching + Homography](https://docs.opencv.org/4.13.0/d1/de0/tutorial_py_feature_homography.html): 특징점 대응과 RANSAC 기반 초기 정렬.
- [OpenCV: Camera Calibration](https://docs.opencv.org/4.13.0/dc/dbb/tutorial_py_calibration.html): 렌즈 왜곡 보정과 카메라 보정.
- [OpenCV: Images stitching](https://docs.opencv.org/4.12.0/d1/d46/group__stitching.html): 파노라마와 affine 기반 스캔 모델의 구분. 이 작업의 폭·피치 제약과 ID 관리는 추가 개발이 필요하다.
- [OpenCV 상세 stitching 예제](https://github.com/opencv/opencv/blob/4.x/samples/python/stitching_detailed.py): 노출 보정·이음새 선택·합성의 구성 요소.
- [Zaragoza et al., CVPR 2013: As-Projective-As-Possible Image Stitching](https://openaccess.thecvf.com/content_cvpr_2013/html/Zaragoza_As-Projective-As-Possible_Image_Stitching_2013_CVPR_paper.html): 공간적으로 변하는 정렬 모델의 참고. 그대로 사용한다고 치수 보존이나 결함 보존이 보장되지는 않는다.
- [Szeliski: Image Alignment and Stitching](https://www.microsoft.com/en-us/research/publication/image-alignment-and-stitching-a-tutorial/): 시차·움직임·노출 차이에 따른 합성 문제.

사진 관찰과 수치는 이 폴더의 진단 결과이고, 피치 ID·물리 좌표·검토 흐름은 본 작업을 위한 설계 제안이다.
